#include <stdio.h>

int main() {
    int A;
 


    printf("Masukkan nilai: ");
    scanf("%d", &A);

            if(A >= 80) {
                printf("nlai anda A");
    } else if (A >= 70 ) {
        printf("nlai anda B");
    } else if (A >= 60 ) {
        printf("nlai anda C");
    } else if (A >= 50 ) {
        printf("nlai anda D");
    } else {printf("nlai anda E");
       
    }


    return 0;
}
